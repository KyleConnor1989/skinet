// or can using type but with = sign
export interface Product {
    id :number;
    name: string;
    description: string;
    price: number;
    prictureUrl: string;
    type: string;
    brand: string;
    quantityInStock: number;
}